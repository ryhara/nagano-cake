<div class="container">
  <div class="">
    <h2><span class="badge bg-light text-dark my-3">ジャンル編集</span></h2>
    <div>
    <%= form_with model:@genre, url: admin_genre_path, method: :patch do |f| %>
        <%= f.label :name, "ジャンル名" %>
        <%= f.text_field :name %>
        <%= f.submit "変更を保存", class:"btn btn-success actions ml-3" %>
    <% end %>
    </div>
  </div>
</div>